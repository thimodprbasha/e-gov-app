import 'package:flutter/cupertino.dart';

class DashBoardInjector extends StatelessWidget {
  const DashBoardInjector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
